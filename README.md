# Verte Tower - Auth Starter

## Quick start

1. Install dependencies
   ```
   npm install
   ```

2. Run dev server
   ```
   npm run dev
   ```

Environment variables are in `.env` (VITE_API_BASE_URL).
